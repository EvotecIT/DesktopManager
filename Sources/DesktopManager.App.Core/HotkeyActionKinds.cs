namespace DesktopManager.App.Core;

/// <summary>
/// Defines action identifiers used by DesktopManager hotkey profile entries.
/// </summary>
public static class HotkeyActionKinds {
    /// <summary>Action that manages the active or selected desktop window.</summary>
    public const string ManageWindow = "ManageWindow";
}
